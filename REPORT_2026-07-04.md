# Отчёт по работе — 04.07.2026

## Что сделано

### 1. Новая анимация UFO (8 кадров)

Получен новый коллаж `тарелка 5.jpeg` (896x1200) с 8 кадрами анимации взлёта тарелки с разными стадиями пламени.

**Анализ структуры коллажа:**
- Изображение 896x1200, формат сетки **2x4** (2 столбца, 4 ряда)
- Белые разделители между кадрами:
  - Вертикальный: x=442-453 (12px)
  - Горизонтальные: y=293-303, y=595-606, y=898-909

**Нарезка кадров** (`slice_new_ufo.py`):
| Кадр | Координаты | Содержание |
|------|-----------|-----------|
| 00 | (0,0)-(441,292) | Прямое пламя вниз |
| 01 | (0,304)-(441,594) | Hourglass пламя |
| 02 | (0,607)-(441,897) | Шарообразный взрыв |
| 03 | (0,910)-(441,1199) | Тонкое пламя влево |
| 04 | (454,0)-(895,292) | Вихревое пламя (75%) |
| 05 | (454,304)-(895,594) | Искры + взрыв (40%) |
| 06 | (454,607)-(895,897) | Пламя вправо (60%) |
| 07 | (454,910)-(895,1199) | Прямое пламя (85%) |

Все кадры масштабированы до **320x240**, центрированы на чёрном холсте.

### 2. Удаление текстовых подписей

С оригинального коллажа на кадрах остались текстовые подписи ("Frame 1", "75%", "40%" и т.д.).
Скрипт `remove_text.py` закрасил углы чёрным:
- Правый верхний: ~100x55px
- Правый нижний: ~100x45px
- Левый нижний: ~100x45px

### 3. Устранение артефактов (полос)

**Проблема:** На экране CYD были видны тонкие полосы — горизонтальная сверху и вертикальная слева.

**Причина:** Остатки белых разделительных линий коллажа, которые попали в кадры при нарезке. JPEG-сжатие размывало границы, и 1-2px белых пикселей оставались на чёрном фоне.

**Решение** (`fix_margins.py`): На ВСЕХ 8 кадрах закрашено чёрным:
- 20px слева по всей высоте
- 20px сверху по всей ширине

### 4. LVGL — устранение артефактов рендера

Добавлены явные параметры в animimg-виджет:
```yaml
- animimg:
    pad_all: 0
    border_width: 0
    outline_width: 0
    bg_opa: 0
```
Это исключает дефолтные рамки/отступы LVGL, которые могли добавлять линии.

### 5. Статический IP

Добавлен статический IP для надёжности:
```yaml
wifi:
  manual_ip:
    static_ip: 192.168.1.16
    gateway: 192.168.1.1
    subnet: 255.255.255.0
```

### 6. Ограничение Flash

ESP32 4MB flash: максимальный раздел приложения — **1.79MB**.
- 1 кадр 320x240 RGB565 = **150KB**
- В прошивку влезают **4 кадра** (~600KB данных)
- Используются кадры: **00, 03, 04, 07** (самые выразительные)

Попытка использовать huge_app.csv (3MB) не удалась — ESPHome не принимает формат раздела.

## Использованные скрипты

| Скрипт | Назначение |
|--------|-----------|
| `slice_new_ufo.py` | Нарезка коллажа 2x4 на 8 кадров 320x240 |
| `remove_text.py` | Удаление текстовых подписей с кадров |
| `fix_margins.py` | Закраска артефактов (20px чёрные поля) |
| `analyze_collage.py` | Анализ структуры коллажа (белые линии) |

## Текущее состояние

- **Прошивка:** `cat_litter_v2.yaml` — стабильна
- **IP:** 192.168.1.16 (статический)
- **Кадры:** 4 из 8 (лучшие) в прошивке
- **Анимация:** плавная, без полос и артефактов
- **Все кадры:** `ufo_anim/ufo_new_frames/` (8 штук, чистые)

## Файлы проекта

```
C:\ESP32\
├── esphome/
│   ├── cat_litter_v2.yaml          # Основной конфиг
│   ├── ufo_anim/ufo_new_frames/    # 8 чистых кадров 320x240
│   └── kot_320x240.png             # Картинка кота
├── ufo_anim/
│   ├── тарелка 5.jpeg              # Исходный коллаж
│   ├── ufo_new_frames/             # 8 кадров (рабочая копия)
│   └── frames_fullscreen/          # Старые кадры (8 штук)
└── slice_new_ufo.py                # Нарезка коллажа
```
